#include <iostream>

using namespace std;

int main(){
	int h, t, v, x;
	h = 10000;
	t = 500;
	v = 50;
	x = 10;

	if (h / t < x)
	{
		tMin = 0;
		tMax = h/x;
	}
		
	else
	{
		tMin = (n - x*t) / (v + x)
		tMax = 
	}


	//cin >> h >> t >> v >> x;
	
	double minspeed = (h + x * t) / (v - x);
	cout << minspeed;
	
}
